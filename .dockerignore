# Secrets — must never reach the build context / Cloud Build
.env
.env.*
!.env.example

# Git metadata
.git
.gitignore

# Files not needed inside the image
README.md
docker-compose.yml
*_test.go

# OS / editor
.DS_Store
.idea/
.vscode/
